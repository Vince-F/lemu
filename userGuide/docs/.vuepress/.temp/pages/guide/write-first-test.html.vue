<template><div><h1 id="write-your-first-test" tabindex="-1"><a class="header-anchor" href="#write-your-first-test" aria-hidden="true">#</a> Write your first test</h1>
<h2 id="the-basics" tabindex="-1"><a class="header-anchor" href="#the-basics" aria-hidden="true">#</a> The basics</h2>
<p>Go to the &quot;Tests&quot; section (in the lesft menu), and add a new test by cliking &quot;Add&quot; button on top of the tests list, or the &quot;Add test&quot; button in the center of your screen (if no test is opened). Then select the test you have just created.</p>
<p>A test, or scenario, consists of one endpoint page (the on you want to test). You define it in the &quot;url&quot; field. You should also defined a unique &quot;label&quot; to identify your test.</p>
<p>&quot;label&quot; and &quot;url&quot; are the only required field for your test. You can add more field to achieve more advance tests. Refer to the <a href="/guide/test-view">tests</a> section.</p>
<h2 id="running-your-test" tabindex="-1"><a class="header-anchor" href="#running-your-test" aria-hidden="true">#</a> Running your test</h2>
<p>Now that you have your first test you can run it. Either you click on the &quot;run test&quot; button in the toolbar to run them all, or you click on the &quot;play&quot; button on the test view header to run this single test.</p>
<h2 id="approving-your-first-test" tabindex="-1"><a class="header-anchor" href="#approving-your-first-test" aria-hidden="true">#</a> Approving your first test</h2>
<p>After the test have run, if everything run until the end, you now have one or more images as a test result. If they are okay with you you can set them as a 'reference' by approving them.</p>
<p>Approving can be done from the report view. See the <a href="/guide/report">report section</a>. It can also be done from the <a href="/guide/test-view">test view</a> for a single test.</p>
</div></template>


